[package]
name = "vault-check"
version = "0.1.0"
edition = "2021"
rust-version = "1.87"

[dependencies]
regex = "1.12"
yaml-rust2 = { version = "0.11", default-features = false }

[profile.release]
strip = true
